Contains 5 versions of code used for a 256x256 image and a file containing code to run some standardised encryption tests
Image_encryption_AttemptX shows each version of the code that encrypts with the raw signal 
Encryption_Testing_FilteredSignal_NIST_ENT filters the signal before encrypting 
